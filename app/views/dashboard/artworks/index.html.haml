%table
  %thead
    %tr
      %th Name
      %th Desc
      %th Artist
      %th Images
      %th
      %th
      %th

  %tbody
    - @artworks.each do |artwork|
      %tr
        %td= artwork.name
        %td= artwork.desc
        %td= artwork.artist_id
        %td= artwork.images

