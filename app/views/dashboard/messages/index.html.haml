
%table
  %thead
    %tr
      %th Send from
      %th Send to
      %th Content
      %th
      %th
      %th

  %tbody
    - @messages.each do |message|
      %tr
        %td= message.send_from_id
        %td= message.send_to_id
        %td= message.content
